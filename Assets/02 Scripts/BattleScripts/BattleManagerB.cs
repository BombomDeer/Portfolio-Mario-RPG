using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleManagerB : MonoBehaviour
{
    List<CharacterB> characters;
    List<Attack> attacks;
    // Start is called before the first frame update
    void Start()
    {
        AddCharactersToList();
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
